package theory;

@SuppressWarnings("ALL")
public class DeleteOperation {

    /*
    *
    * Para implementar uma operação de DeleteByID foi sobrescrito o método
    * onde a assinatura é um void que recebe um integer em referencia ao id
    * em seguida foi copiado a mesma estrutura do insert para alterar somente algumas coisas
    * alterado a instrução SLQ para DELETE FROM ...
    * Depois de executado o executeUpdate() verificamos se foi realmente deletado com uma
    * condição e lançamos uma exceção com a informação da situação.
    *
    * Depois criado um ProcessCustomer3 para executar o método de exclusão por id passando um registro
    *
    *
    * */
}
